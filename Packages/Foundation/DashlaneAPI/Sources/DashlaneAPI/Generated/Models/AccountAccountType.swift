import Foundation

public enum AccountAccountType: String, Codable, Equatable, CaseIterable {
    case masterPassword = "masterPassword"
    case invisibleMasterPassword = "invisibleMasterPassword"
}
